type Book @model {
  id: ID!
  name: String!
  bookmarks: [Bookmark] @connection
}

type Bookmark @model {
  id: ID!
  url: String!
  description: String
  tags: [String]
  book: Book @connection
}

type Tag @model {
  id: ID!
  name: String!
  type: String
}